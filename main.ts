function x_y_handler (name_sprite: string, x: number, y: number) {
    if (true) {
    	
    } else {
    	
    }
}
function create_sprite (name: string, x: number, y: number) {
    spritelist.push(name)
    x_y_handler(name, x, y)
}
let spritelist: string[] = []
spritelist = []
basic.forever(function () {
	
})
